Dubbo和springCloud的比较

	1.Dubbo使用RPC通讯协议

	2.Spring Cloud使用HTTP协议的REST API。

	3.Dubbo服务提供方与消费方通过接口的方式依赖,Dubbo为每个微服务定义各自的interface接口，并通过持续集成发布到私有仓库中。调用方应用对微服务提供的抽象接口存在强依赖关系，开发、测试、集成环境都需要严格的管理版本依赖

	4.服务提供方和服务消费方通过 Json 方式交互，因此只需要定义好相关 Json 字段即可，消费方和提供方无接口依赖。通过注解方式来实现服务配置，对于程序有一定入侵。

